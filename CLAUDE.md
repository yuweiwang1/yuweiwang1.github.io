# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

This is a GitHub Pages personal website for Yuwei Wang, hosted at `yuweiwang1.github.io`. Currently a bare static site with no build tooling, frameworks, or dependencies — just plain HTML/CSS/JS files served directly by GitHub Pages.

## Deployment

Changes pushed to the `main` branch are automatically deployed via GitHub Pages. There is no build step; files are served as-is.

## Development

Open `index.html` directly in a browser to preview, or use any local static server:

```
python3 -m http.server 8000
```
